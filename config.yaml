# Clawdbot Gateway Configuration
gateway:
  mode: local
  host: 0.0.0.0
  port: ${PORT:-18789}
  
# Agent configuration
agents:
  main:
    model: emergent-claude/claude-sonnet-4-5
    name: Partner
    description: 24/7 AI Assistant

# Logging
logging:
  level: info
